apiVersion: v1
kind: Service
metadata:
    name: wasimjenkinswithdockercomposeup
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: wasimjenkinswithdockercomposeup